<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- FileProvider resolves individual files from filesDir; share intents grant per-file URIs only -->
    <files-path name="logs" path="." />
</paths>
